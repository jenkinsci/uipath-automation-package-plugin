<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry field="testProjectPath" title="${%TestProjectPath}">
        <f:textbox default="project.json"/>
    </f:entry>
    <f:entry field="environments" title="${%Environment}">
        <f:textbox />
    </f:entry>
</j:jelly>